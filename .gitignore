# ── Build outputs ─────────────────────────────────────────────────────────────
build/
cmake-build-*/
out/
dist/
*.o
*.a
*.so
*.dylib
*.dll
*.exe

# ── CMake ─────────────────────────────────────────────────────────────────────
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
CTestTestfile.cmake
_deps/
install_manifest.txt

# ── Flutter ───────────────────────────────────────────────────────────────────
flutter_app/.dart_tool/
flutter_app/.flutter-plugins
flutter_app/.flutter-plugins-dependencies
flutter_app/.packages
flutter_app/build/
flutter_app/pubspec.lock
flutter_app/android/.gradle/
flutter_app/android/local.properties
flutter_app/android/key.properties
flutter_app/ios/Pods/
flutter_app/ios/Podfile.lock
flutter_app/ios/.symlinks/
flutter_app/ios/Flutter/Generated.xcconfig
flutter_app/ios/Flutter/flutter_export_environment.sh
# Per-developer platform runner dirs. macOS Runner.xcodeproj/ stays tracked
# because it carries the build phase that copies libbettersend_core.dylib into
# the app bundle (see commit 5360a84).
flutter_app/windows/
flutter_app/linux/
*.g.dart
*.freezed.dart

# ── IDE / Editors ─────────────────────────────────────────────────────────────
.vscode/
.idea/
*.xcuserstate
*.xcworkspace/xcuserdata/
*.xcodeproj/xcuserdata/
xcuserdata/
.vs/
*.suo
*.user

# ── OS ────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
*~

# ── Logs & Debug output ───────────────────────────────────────────────────────
*.log
logs/
bettersend_debug.log

# ── Android ───────────────────────────────────────────────────────────────────
*.apk
*.aab
*.keystore
local.properties

# ── iOS / macOS ───────────────────────────────────────────────────────────────
*.ipa
*.dSYM/
DerivedData/

# ── Windows ───────────────────────────────────────────────────────────────────
*.msix
*.msixbundle

# ── Test artifacts ────────────────────────────────────────────────────────────
Testing/
CTestCostData.txt

# ── Secrets / Config ──────────────────────────────────────────────────────────
.env
*.pem
*.p12
*.p8
google-services.json
GoogleService-Info.plist

# ── FetchContent cache (can be re-downloaded) ─────────────────────────────────
# Uncomment if you want to exclude downloaded dependencies from git
# cpp_core/build/_deps/

# ── OpenWolf ──────────────────────────────────────────────────────────────────
.wolf/

# ── Local caches (clangd index, screenshots, scratch) ────────────────────────
.cache/
